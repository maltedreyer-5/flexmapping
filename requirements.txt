# FlexMapping runtime dependencies.
# This file is the single source of truth for Python dependencies.
# Versions are pinned so that a checkout installs the set that was tested.

# --- Web framework --------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# --- Authentication -------------------------------------------------------
# Argon2 for password hashing; itsdangerous signs the session cookie.
argon2-cffi==25.1.0
itsdangerous==2.2.0

# --- Database -------------------------------------------------------------
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
# psycopg2 is LGPL with exceptions, unlike the rest of this list. It is used
# only for the synchronous engine (Alembic and the health check).
psycopg2-binary==2.9.13
alembic==1.20.0

# --- Queue ----------------------------------------------------------------
redis==8.1.0

# --- HTTP clients ---------------------------------------------------------
httpx==0.28.1
aiohttp==3.14.3

# --- HTML processing and Markdown -----------------------------------------
beautifulsoup4==4.15.0
lxml==6.1.3
html5lib==1.1
markdownify==1.2.3
markdown2==2.5.5

# --- Templates ------------------------------------------------------------
jinja2==3.1.6

# --- Configuration and validation -----------------------------------------
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
pyyaml==6.0.3

# --- Logging --------------------------------------------------------------
structlog==26.1.0

# --- Date and time --------------------------------------------------------
python-dateutil==2.9.0.post0
pytz==2026.3.post1

# --- Utilities ------------------------------------------------------------
click==8.5.0
requests==2.34.2
cryptography==50.0.1
